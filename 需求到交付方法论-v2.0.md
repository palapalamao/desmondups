# 需求到交付方法论 v2.0

| 项 | 内容 |
|---|---|
| 文档名称 | 需求到交付方法论（通用 · 系统视角） |
| 版本 | V2.0 |
| 日期 | 2026-09-18 |
| 定位 | **与产品无关、与技术栈无关、与行业无关。** 本方法论管的是"从意图到可信系统"的通用过程；文中出现的全部项目名、工具名、文件名、命令均为**实例注解**——用来证明原则落过地，可被任何等价物替换，不是方法本体 |
| 溯源 | 提炼自两个真实交付项目的实战：careBalance（医院成本分摊平台）全套文档；energyMatrix（能源管理平台）工程约定、特性级 spec/plan、证据链、发布资料 |
| 适用范围 | 一切对质量与可信度有要求的系统交付。在有安全/合规硬约束、多外部系统对接、需多方会签、交付物需审计追溯的复杂场景下价值最大；简单场景可按门禁清单裁剪，但公理不可裁剪 |
| 上游版本 | 需求到交付方法论-v1.0.md（保留，不覆盖） |

## 定位 · 三个系统视角声明

**一、方法论不绑任何产品。** 它回答的是所有产品交付共有的五个问题：怎么把领域搞清楚、怎么把承诺写死、怎么让机器守住承诺、怎么按现实排程、怎么证明交付成立。换成任何行业、任何技术栈，阶段、机制、清单原样成立，只需替换实例。

**二、交付物是一个系统，不是一堆源码。** 完整交付物有六个要素：**代码、数据、文档、环境状态、流程机制、组织获得的能力**。六者同为交付物本体，同等地受红线约束、被机器审计、有终审标准。只验代码不验其余五样，是系统视角缺失，不是"差不多行了"。

**三、方法论在尺度上自相似（递归适用）。** 同一个循环——**想清楚 → 写死 → 机器守 → 对照收**——在项目尺度是七阶段流程，在特性尺度是三段闸门微流程，在一次缺陷修复尺度是"复现 → 修 → 回归测试 → 证据"。大闭环套小闭环，每个尺度都过门禁，不存在"这个太小不走流程"或"这个太大流程管不了"。

## V2.0 修订要点（相对 V1.0）

| # | 修订（通用原则） | 原则来源（实例注解） |
|---|---|---|
| 1 | 新增第三条公理：**交付物整体性**——交付物六要素同权受审（"数据即代码"的一般化） | 能源平台的数据结构红线与实时值红线，终审标准同时覆盖结构与数据 |
| 2 | 阶段 0 由"技术底座盘点"扩为**环境与底座盘点**，新增**陷阱登记表**：探测不只探能力，还要探坑；踩坑即成文 | 工程约定文件中的「关键机制（实测结论）」与坑登记 |
| 3 | 新增**特性级微流程**（第一部分补）：宏观七阶段在特性尺度的递归实例化，分段确认 | 特性开发按 需求→设计→开发 三段闸门推进，每段提交后停下等确认 |
| 4 | 阶段 2 增加**结构红线**写法：必备约束表 + 推导链 + 冲突即报错 | 数据建模中"每层记录必备归属标签"，显式优先、自动推导、冲突构造期报错 |
| 5 | 阶段 4 增加**版本联动纪律**与**双格式文档同步校验** | 一次变更多处版本号成组升级；权威源改后派生物重新生成并抽文本反验 |
| 6 | 阶段 6 红线机器化扩为**双轨**：源码红线走 CI，数据/运行态红线走审计脚本；新增**初始数据是一等交付物**、**红线生命周期（存量清零）** | 审计脚本终审全绿标准；建数脚本幂等可重建；千余条存量数据当日回填清零 |
| 7 | 阶段 7 增加**证据链交付**与**未验证项明示**；对外资料需**口径统一来源** | 证据目录（可复跑脚本+截图+终审）；发布资料索引的「关键产品事实」节 |
| 8 | 约束硬化链从四层扩为**五层**（宣言→原则→契约→机器→数据/运行态审计） | 同一条结构红线在约定文件、详设、创建层报错、单测、审计终审五处各出现一次 |

---

## 总纲

> 先把领域的失败模式和否决者搞清楚（业务层）→ 把口径和语义固化（概念层）→ 把边界和守门机制立起来（逻辑层）→ 把一切写成无歧义契约（契约层）→ 按组织就绪度排程（计划层）→ 用机器守住红线写代码（实现层）→ 对照开头写下的标准交付（验收层）。

这条链与技术无关：它做的事是把**意图**逐层转化为**可被机器验证的承诺**，再把承诺兑现为**有证据的系统**。

三条公理：

1. **可信度不来自永远正确，而来自永远诚实**——缺口不藏、不足标出、失败可见、未验证项明示。
2. **承诺必须可被机器验证**——口头承诺不可靠，人工评审会疲劳，自动检查不会。
3. **交付物整体性**（V2.0 新增）——交付的是一个可运行、可审计、可重建的系统：代码、数据、文档、环境状态、流程机制、组织能力，六要素同为交付物本体。任何一项残缺，交付不成立；代码全绿而数据残缺，同样不成立。

---

## 第一部分 · 七阶段流程

每阶段按固定格式给出：做什么 / 产出 / 门禁（进入下一阶段的放行条件）。

### 阶段 0 · 环境与底座盘点（开工前）

**做什么**

- **技术底座**：确认项目站在什么平台/内核/依赖之上，实地探测其真实能力（版本、接口、行为），不凭记忆写契约。
- **既有资产**：下"不重复造轮子"的纪律决定——哪些复用、哪些扩展、哪些全新。
- **组织环境**：摸清决策链、会签习惯、数据所有者、外部接口的对方团队——组织现实与技术现实同为约束。
- **建立陷阱登记表（V2.0 新增）**：探测不只探"能做什么"，还要探"怎么死"。每发现一个坑（解析时机、语义陷阱、时区行为、过滤语义不可靠），立即以「实测结论」形式写入工程约定文件，注明触发条件与正确写法。登记表随项目终身维护，**踩坑即成文，不过夜**。

**产出**：能力复用清单；底座探测记录；组织环境备忘；陷阱登记表。（实例注解：某平台工程约定文件记录——模板继承是构造期硬解析而非软失败、删除记录须用专用函数而非标记字面量、时区样本必须走特定写入口否则错位 8 小时、日期等值过滤不可靠需改在客户端过滤）

**门禁**：底座关键能力逐项验证存在；对底座"不改写"的边界已书面确认；已知陷阱全部成文。

### 阶段 1 · 业务建模（需求文档）

**做什么**——按固定顺序回答五个问题：

1. **这个领域和我熟悉的领域有什么结构性差异？** 产出差异对照表，每条差异必须推导出一个设计结论。（实例：医院 vs 商业建筑七维差异表）
2. **这个领域最常见的死法是什么？** 产出失败模式清单，后续全部设计围绕规避它们。（实例：① 费用分不到科室 → 财务不认账 → 平台沦为一块大屏；② 节能动作触碰安全红线 → 被一票否决 → 组织从此不敢用平台）
3. **不可妥协的原则是什么？** 产出设计铁律。
4. **谁在用、解决什么问题？** 产出角色地图 + 功能模块表，每个模块按「使用者 / 解决的问题 / 核心功能」三段式描述，模块编号即需求条目主键。
5. **怎么算交付成功？** 量化验收标准**此时写死**，不是最后补。

**V2.0 新增——决策记录格式**：每个重要需求除正文外，产出一张**已确认决策表**，三列：决策点 / 结论 / 备选（已否决或二期）。**否决项与否决理由必须留痕**——它们阻止后来者重开已关闭的争论。（实例：某监测屏决策表——单屏多页签（否决多屏并列）、复用既有告警体系（否决新建）、只监测不控制、既有列表页保持不动（否决合并））

**V2.0 新增——演示先行**：视觉/交互敏感的需求，先做静态演示（纯前端模拟数据）获决策层确认，再进详设。演示确认成本远低于开发返工。

**产出**：《业务方案》（= PRD）+ 决策记录表 + （如适用）静态演示。

**门禁**：决策层签字；验收标准双方确认；每条失败模式都有对应规避设计；决策表无悬空争论。

### 阶段 2 · 概念与口径治理

**做什么**

- 把业务世界建成语义模型：对象、属性、枚举、关系，一域一文件，可直接落成代码。
- **把争议口径抢在写代码前固化**：枚举所有口径争议项（分母算不算 X、收入含不含 Y），以「口径确认单」形式多方会签落库；未会签的指标拒绝计算或标注参考值。
- 把治理要求编译进数据模型：约束必填依据文号、关键对象强制只读标记、外部编码必须经映射层。
- **V2.0 新增——结构红线**：对核心数据的结构完整性约束，以「红线表」形式写死每类记录必须具备什么，并设计**推导链与冲突策略**。通用模式：

  | 记录类型 | 必备约束（实例：归属标签） |
  |---|---|
  | 最底层记录 | 直接上级 + 各级归属 |
  | 中间层记录 | 各级归属 |
  | 上层记录 | 顶层归属 |

  落地三级：① **显式指定优先**；② 由关联对象**自动推导**（实例：由所属分区推导所属楼层）；③ 显式与推导冲突**构造期即报错**，不静默取其一。自动继承链必须保持可用，不得退化为手工传参。

**产出**：语义库 + 建模铁律 + 口径确认单模板 + 结构红线表。

**门禁**：语义模型 lint 通过；关键口径全部有会签路径（人可未签，机制必须先建好）；结构红线表覆盖全部核心记录类型且每条有强制机制（推导或报错），不只是文字要求。

**要点**：指标算错通常不是算法错，是口径没签。口径治理属于需求阶段的尾巴，不属于开发阶段。

### 阶段 3 · 逻辑设计（详设）

**做什么**

1. **先划边界**：复用边界表逐项列出「直接复用 / 继承扩展 / 全新开发」，并写明负需求（不做什么）。**零新增是首选答案**——新功能优先复用既有通道与既有引擎，新增接口/新体系需要论证为何复用不成立（实例：某监测功能后端接口零新增，读数复用既有历史通道、告警复用既有规则引擎）。
2. **再定分层**：配硬约束（下层访问唯一出口化）。
3. **识别系统的一票否决者**：把安全/合规否决逻辑独立成守门引擎——fail-closed（拿不到证据即拒绝）、否决是正常业务结果（非异常）、通过与否决全量留痕。
4. **定并发与事务模型**：调度串行化、幂等键、无跨记录事务时的补偿（红冲）策略、长任务进度回写。
5. 画关键流程时序图；定非功能指标（容量、性能、数据完好率）。

**产出**：《详细设计说明书》+ 图包（图源码与渲染图双格式）。

**门禁**：每个需求模块都能追溯到设计元素；每条铁律都有对应的架构机制而不只是口号；每个「复用」结论注明复用的既有元素名称，每个「新增」结论注明复用不成立的理由。

### 阶段 4 · 契约化（开发文档）

**做什么**——把设计展开成无歧义契约，标志是"开发拿到任何一篇都能直接写代码，不需要回来问"：

- 入口一页纸：N 条绝对不能违反的红线 + 仓库结构 + 命名速查。**V2.0 扩充入口文件三区**：① 红线区（含生效日期与存量状态）；② 关键机制区（实测结论，修改链路前必读）；③ 运维手册区（重建/恢复的标准操作顺序）。
- 编码规范先行；领域类构造期不变式（非法状态构造即抛错）；持久层唯一出口 + 写入校验表；核心算法伪代码 + 测试用例表；接口契约定死（分页、排序、幂等、错误响应格式）；错误码全表独立成篇；前端契约（路由、权限矩阵、强制渲染规则）。
- **V2.0 新增——版本联动纪律**：同一逻辑版本散落在多个制品与文档中时，一次变更必须**成组升级**并列入契约——各制品版本号、模型库版本、各文档版本行与修订记录，一处不漏。契约里必须有「版本联动清单」防止漏升。
- **V2.0 新增——派生文档同步校验**：一种格式为权威源、其余格式为派生物时，派生物必须重新生成并**抽文本反向验证**（关键词出现次数、旧版本号零残留），不允许"改了源忘了派生"。（实例：「新版本号×6、新术语×7、旧词 0 残留」抽文本验证）

**产出**：开发文档全套 + 版本联动清单。

**门禁**：任一契约无歧义；每条红线都标明了由哪层机制（断言/评审/自动检查）强制执行；版本联动清单逐项勾选。

### 阶段 5 · 计划与排程

**做什么**

- 任务拆解到"可直接开 issue"的粒度，按里程碑组织。
- 排序依据是**技术依赖 × 组织就绪度**两个维度，不只是技术依赖。（实例：守门引擎技术上是核心，却排第四期——它依赖"关键区域清单三方会签"这件需要数月的组织工作；此前走人工流程兜底）
- 每个阻塞风险单独列表：风险 / 阻塞的里程碑 / 缓解方案 / 谁去推动。
- **V2.0 新增——项目级里程碑之下，按特性级微流程滚动推进**（见第一部分补）：宏观排程管"谁先谁后"，微流程管"每个特性怎么落地"，两层不混写。

**产出**：里程碑表 + 阻塞风险表。

**门禁**：每个里程碑既标技术依赖也标组织依赖；每个风险有兜底路径。

### 阶段 6 · 实现（编码纪律）

1. **红线机器化——双轨制（V2.0 扩展）**：
   - **源码红线**走自动静态检查，进构建必经环节——构建失败比人工评审可靠。
   - **数据/运行态红线**走**可重跑的审计脚本**探活体状态。凡交付物包含持久数据或运行环境，红线就同时存在于库里和运行态里，不止源码里：结构审计（全量校验必备约束）、内容审计（终审全绿标准——结构全过、关键值零缺失、统计期数与日历一致）。审计脚本入版本库，终审必须留运行记录。
2. **防御性默认（fail-closed）**：必填缺失即抛错不兜底；取不到实测值视为不满足；找不到归属按最高风险处理；不存在"先执行不冲突部分"的折中。
3. **数据诚实**：账目不平衡即阻断；缺口永不摊入；未确认数据强制标注"参考值"；人工填报必须标来源等级。
4. **历史不可改**：版本化（只新增带新生效期的版本）；修正走冲正不覆盖历史；保留原始外部编码；一切写操作幂等——保证任意历史账期可无损重算。
5. **工程纪律**：引擎无状态（上下文显式传入）；持久层唯一出口；错误统一收敛为单一类型，对外不暴露堆栈；调度统一串行化，不自建并发池。
6. **降级路径先于主路径**：外部依赖不可达时的人工兜底方案是设计的一部分，流程不中断。
7. **V2.0 新增——初始/演示数据是一等交付物**：凡系统依赖初始数据、演示数据、迁移数据，其构建脚本必须满足——
   - **幂等可重跑**（重复执行不产生重复记录）；
   - **确定性**（合成数据用确定性种子，可复算复验）；
   - **有重建手册**（环境被重建后按固定顺序重跑即可全恢复）；
   - **动活体环境的数据操作单独列示并经确认**（不是代码提交的附属品）。
8. **V2.0 新增——运行时缺陷沉淀为回归测试**：真机/运行时验收发现的每个缺陷，修复时必须附带回归测试，根因写入提交记录（实例：依赖库升级后某序列化行为变化导致整页空数据——改判定逻辑 + 补回归测试 + 提交信息写明根因）。依赖升级后的行为变化只能靠运行时实测暴露，**单测绿不等于真机绿**。

**门禁**：源码红线检查全部上线且为构建必经；数据审计脚本全绿且有运行记录；初始数据可按手册确定性重建；核心算法测试用例全覆盖契约层用例表。

### 阶段 7 · 验证与交付

**分层验证**：单元（不变式与算法）→ 集成（关键业务全链路各走一遍）→ 接口（契约 + 幂等 + 错误码）→ 现场验收。

**V2.0 新增——证据链交付**：验收不以"通过了"一句话收尾，而以**证据目录**收尾：

- 每类验收有**可重跑脚本**（探针/审计/终审）+ 运行结果留档 + 关键画面截图；
- 复杂数据场景附**口径说明**（目标口径表、参考依据、执行步骤可复跑、验收结果实测表）；
- 部署留痕（部署脚本 + 结果与时间戳留档）。

**V2.0 新增——未验证项明示**：因环境限制未完成的验证，必须显式列出"未验证什么、已做的替代验证是什么、留给谁怎么验"，禁止默认已绿。（实例：「浏览器不可用，页面目检留待人工打开确认；已完成替代验证：路由与菜单在列、版本正确、引擎跑通」）

**收官动作**：

1. **验收标准回收**：阶段 1 写下的每条标准逐条对账。
2. **权限与能力移交**：客户获得全部管理权限；强制审计操作现场演示可追溯。
3. **文档回路闭合**：开发中与上游文档的一切冲突回写上游，不留两套真相。
4. **V2.0 新增——对外资料口径统一**：一切对外材料（销售版/技术版/通讯稿/附件）以一份「关键产品事实」清单为**口径统一来源**（产品能力数字、红线口径、规格参数只在这一处维护），每份材料注明来源（自创/复用/沿用）与校验方式（渲染目检/结构校验/公式校验），中间产物目录与正式交付物分离、可整目录删除。

**门禁**：验收标准全部签字回收；压测与容量验证达标；文档链无未回写的漂移；证据目录可独立复跑；未验证项清单为空或已移交责任人。

---

## 第一部分补 · 特性级微流程（V2.0 新增）

**方法论是尺度自相似的。** 七阶段是项目尺度的实例化；单个特性/需求的落地走**微流程**——同一个"想清楚 → 写死 → 机器守 → 对照收"循环在特性尺度的实例化。微流程把阶段闸门小型化，尤其适用于 AI 结对开发：每段结束提交并**停下等确认**，不一口气跑到底。

### 三段闸门

```text
段 1 · 需求   更新需求文档 + 写 spec（设计决策记录）→ 提交 → 停下等确认
段 2 · 设计   详设文档 + 模型 + 接线（菜单/词条等）+ plan（任务清单）→ 提交 → 停下等确认
段 3 · 开发   代码 + 测试 + 版本联动 + 数据 + 真机目检 + 证据 → 提交 → 收尾
```

（实例注解：某监测屏——段 1 提交需求章节；段 2 提交详设+模型新增+菜单；段 3 提交实现+版本升级，随后证据提交建数与部署留档）

### spec（设计决策记录）骨架

```text
# <特性名> · 设计决策记录
> 日期 / 分支 / 当前阶段 / 上游需求文档及版本
## Goal                    # 一段话+编号子需求
## 已确认决策              # 表：决策点 / 结论 / 备选（已否决·二期）
## 模型缺口与对策          # 表：缺口 / 对策 / 落点（含"已存在无需新增"清单）
## 接口预期                # 优先结论=零新增
## 版本                    # 本次联动的版本号清单
## 阶段闸门                # 各段范围+停下等确认的硬约束
## 段 N 记录               # 执行后回写：提交号、实测结论（spec 是活文档）
```

### plan（任务清单）骨架

- 复选框任务按段分组，**每段最后一项固定为「提交并停下等确认」**；
- 任务含验证步骤（测试命令、编译、构建、真机目检）而不只是"写代码"；
- 动活体环境的数据操作单独成组并标注"经确认后执行"；
- 未完成的验证保留未勾选状态并写明原因——**plan 的勾选状态即诚信记录**。

### 微流程纪律

1. **决策表否决项留痕**——防止重开已关闭的争论。
2. **静态演示先行**——视觉敏感需求先确认演示再开发。
3. **零新增优先**——能复用既有通道/引擎/域就不新增，新增须论证。
4. **执行记录回写 spec**——提交号与实测结论回写同一文档，spec/plan 不全开新文件。
5. **任何一段不得越段动工**——需求段不动模型，设计段不动页面代码。

---

## 第二部分 · 贯穿机制（灵魂）

| 机制 | 含义 | 实例注解 |
|---|---|---|
| **约束硬化链（V2.0 扩为五层）** | 每条核心约束必须在五层各出现一次：宣言（铁律）→ 原则（详设）→ 契约（断言/红线）→ 机器（自动检查/测试）→ **数据/运行态审计（审计脚本+终审）**。人读前两层，评审看第三层，机器守第四层，运行态红线靠第五层 | 某结构红线：约定文件宣言 → 详设建模原则 → 创建层缺约束即报错 → 单测 → 审计脚本 + 终审全过（千余条记录零缺失） |
| **验收前置与回收** | 成功标准在需求阶段写死，中间所有设计决策用它裁决，交付阶段以**证据链**形式逐条回收 | 业务方案验收章 → 测试计划验收表 → 终审脚本全绿标准 + 证据截图 |
| **真相单一、权威下沉、冲突回写** | 每层文档只说自己该说的并声明不覆盖什么；越靠近代码越权威；冲突必须回流修正上游；**对外材料以单一事实清单为口径源** | "冲突时以开发文档为准并回写上游"；发布资料索引的「关键产品事实」节统一全部对外口径 |
| **红线生命周期管理（V2.0 新增）** | 每条新红线必须带完整生命周期：**生效日期 → 存量缺口处置（回填/清理）→ 审计脚本 → 终审证据 → 运维手册**。只宣言不清存量的红线是假红线 | 某结构红线当日生效：千余条存量缺口当日全量回填、失效孤儿记录清理、终审全过、脚本与证据落证据目录；另一红线全量回填（部分取历史末值 + 部分确定性合成） |

---

## 第三部分 · 五层文档漏斗（文档流水线）

```text
① 业务层   为什么做 / 为谁做 / 不做什么 / 怎么算成   → 《业务方案》（= PRD）+ 决策记录表
② 概念层   业务世界里有什么对象、什么口径、什么结构红线 → 语义模型 + 口径确认单 + 红线表
③ 逻辑层   系统怎么组织、职责怎么切分               → 《详细设计》：架构、边界表、时序、非功能
④ 契约层   精确到可编码的接口与规则                 → 开发文档：规范、领域、算法、接口、错误码、前端、版本联动
⑤ 执行层   谁先谁后、怎么验证、何时算完             → 任务拆解 + 测试计划 + 自动检查 + 数据审计 + 证据目录
```

配套规则：

- **单向承接**：每层只增加一类决策；每层文档开头声明"不覆盖什么"。
- **模块编号即需求主键**：需求 → 设计元素 → 里程碑 → 测试用例，全链可追溯。
- **验收标准在开头写、在结尾以证据形式收**。
- **派生物单向导出**：对客户/决策层的汇报材料从业务方案单向导出，是叶子节点，不参与依赖链；对外材料的事实口径从「关键产品事实」清单单向导出。
- **图源码与语义模型源码入版本库**：源码 + 渲染双格式，可重建。
- **多格式文档以单一权威源为准**，派生格式重新生成后抽文本反向校验。
- **证据与中间产物分离**：可复跑脚本与终审留档入证据目录；生成脚本/提取文本等中间产物单独成目录，可整目录删除不影响正式交付物。

---

## 第四部分 · 注意事项速查清单

| # | 事项 | 做法 |
|---|---|---|
| 1 | 别抄方案 | 先做领域结构性差异分析 + 失败模式复盘，每条差异推出一个设计结论 |
| 2 | 硬约束引擎化 | 独立守门引擎，fail-closed，否决是正常业务结果且全量留痕 |
| 3 | 承诺可验证 | 源码红线自动检查 + 数据/运行态红线审计脚本双轨，不止人工评审 |
| 4 | 数据宁缺毋假 | 缺口/未映射/参考值全部显式标注；不平衡即阻断 |
| 5 | 口径先于算法 | 争议口径启动阶段多方会签固化，未会签拒算或标参考值 |
| 6 | 里程碑看组织就绪度 | 依赖会签/对照表的功能后置，先走人工流程兜底 |
| 7 | 历史不可改 | 版本化 + 冲正 + 幂等 + 保留原始编码，任意历史账期可无损重算 |
| 8 | 边界表格化 | 复用/扩展/全新逐项列清；写明负需求；不改写上游内核；零新增优先 |
| 9 | 集成最小化 | 只读、聚合、文件交付、网络分区、数据结构禁止敏感标识字段 |
| 10 | 防御性默认 | 无证据即拒绝；拿不到归属按最高风险等级处理 |
| 11 | 点表/数据留冗余 | 应急与监管关键数据设计期必留，事后补采代价极高 |
| 12 | 验收前置 | 量化验收标准需求阶段写死，反向定义"完成" |
| 13 | 异常是一等公民 | 错误码全表独立成篇；降级路径先于主路径设计 |
| 14 | 文档有门禁 | 文档 lint、图源码入库、分发快照与工作副本分离、派生格式抽文本校验 |
| 15 | 冗余即风险 | 同一内容多处副本时必须规定权威方并回写同步 |
| 16 | 红线带生命周期 | 生效日期 + 存量清零 + 审计脚本 + 终审证据 + 运维手册，五件齐才算红线落地 |
| 17 | 踩坑即成文 | 环境陷阱当场记入工程约定（触发条件 + 正确写法），不过夜、不靠记忆传承 |
| 18 | 初始数据可重建 | 幂等 + 确定性种子 + 重建手册；动活体环境的数据操作单独确认 |
| 19 | 版本成组联动 | 各制品与文档的版本号、修订记录一次变更多处同升，有联动清单防漏 |
| 20 | 未验证项明示 | 环境限制验不了的，写明"未验什么/替代验证/谁补验"，禁止默认已绿 |
| 21 | 微流程分段确认 | 特性落地三段闸门，每段提交后停下等确认；决策否决项留痕 |
| 22 | 交付物看六要素 | 代码/数据/文档/环境状态/流程机制/组织能力，验收六样同查，不查等于没交付 |

---

## 第五部分 · 阶段检查清单（可直接勾选）

### 阶段 0 放行检查（V2.0 扩）

- [ ] 底座关键能力逐项探测验证存在
- [ ] 复用/扩展/全新决定成文
- [ ] 组织环境（决策链/会签习惯/数据所有者/接口对方）已摸清
- [ ] 陷阱登记表建立，已知坑全部成文（触发条件 + 正确写法）

### 阶段 1 放行检查

- [ ] 领域结构性差异表完成，每条差异有设计结论
- [ ] 失败模式清单完成，每条有规避设计
- [ ] 设计铁律 ≤ 5 条，决策层认可
- [ ] 功能模块表（使用者/问题/功能三段式）完成并编号
- [ ] 量化验收标准双方书面确认
- [ ] 决策记录表完成，否决项与理由留痕
- [ ] 视觉敏感需求已做静态演示并获确认

### 阶段 2 放行检查

- [ ] 语义模型 lint 通过
- [ ] 建模铁律成文
- [ ] 全部口径争议项有确认单与会签路径
- [ ] 治理要求（依据文号、只读标记、映射层）已编译进数据模型
- [ ] 结构红线表覆盖全部核心记录类型，每条有强制机制（推导链/构造期报错）

### 阶段 3 放行检查

- [ ] 复用边界表完成（复用/扩展/全新 + 负需求），新增项均有"复用不成立"论证
- [ ] 分层图 + 跨层访问硬约束成文
- [ ] 守门引擎独立，fail-closed，留痕方案明确
- [ ] 并发/事务/幂等/补偿模型成文
- [ ] 每个需求模块可追溯到设计元素

### 阶段 4 放行检查

- [ ] 红线一页纸发布（含生效日期、关键机制实测结论、运维手册三区）
- [ ] 每个领域类有构造期不变式
- [ ] 持久层唯一出口 + 写入校验表
- [ ] 核心算法伪代码 + 测试用例表
- [ ] 接口契约无歧义（分页/幂等/错误格式）
- [ ] 错误码全表发布
- [ ] 前端权限矩阵 + 强制渲染规则
- [ ] 版本联动清单建立（各制品 + 全部文档）
- [ ] 派生文档的生成规则与抽文本校验方法成文

### 阶段 5 放行检查

- [ ] 任务可直接开 issue
- [ ] 每个里程碑标注技术依赖与组织依赖
- [ ] 阻塞风险表：风险/影响/缓解/责任人
- [ ] 特性级微流程的 spec/plan 模板就位

### 阶段 6 放行检查

- [ ] 源码红线自动检查全部上线且为构建必经
- [ ] 数据/运行态审计脚本全绿，运行记录留档
- [ ] 初始/演示数据脚本幂等 + 确定性 + 有重建手册
- [ ] 核心算法测试用例全覆盖
- [ ] 降级路径已实现并测试
- [ ] 历史重算能力已验证（用旧账期实测）
- [ ] 运行时发现的缺陷均已沉淀回归测试与根因记录

### 阶段 7 放行检查

- [ ] 集成全链路测试通过
- [ ] 验收标准逐条回收签字
- [ ] 证据目录可独立复跑（脚本 + 留档 + 截图 + 口径说明）
- [ ] 未验证项清单为空或已移交责任人
- [ ] 权限移交 + 审计可追溯现场演示
- [ ] 文档冲突全部回写上游，无漂移
- [ ] 对外材料口径与「关键产品事实」清单一致，来源与校验方式逐份注明
- [ ] 交付物六要素逐项验收，无漏项

---

## 附录 A · 文档骨架模板（新项目套用）

> 目录与文件名为参考实例，可按团队惯例替换；**结构关系不可省**——入口一页纸、需求/详设/开发文档分层、演示、证据、发布资料五者分离。

```text
项目根/
  <入口一页纸>.md         # 三区：红线区（生效日期+存量状态）
                         #      + 关键机制区（实测结论/陷阱登记）
                         #      + 运维手册区（重建恢复标准顺序）
  docs/
    01-业务方案           # 阶段 1 产出（= PRD）
    02-详细设计           # 阶段 3 产出（权威源 + 派生格式）
    specs/               # 特性级微流程：设计决策记录（活文档，执行记录回写）
    plans/               # 特性级微流程：分段任务清单（勾选状态即诚信记录）
    demo/                # 静态演示（评审用，先行确认）
    evidence/            # 证据链：可复跑脚本 + 终审 + 截图 + 口径说明 + 部署留档
    releasedoc/          # 对外发布资料 + 索引（对照清单 + 关键产品事实口径源）
    dev/                 # 阶段 4 产出（开发文档）
      README.md          # 索引与阅读顺序
      00-conventions.md  # 编码与提交规范
      01-architecture.md # 分层 + 边界表 + 并发
      02-<domain>-lib.md # 语义模型
      03-domain.md       # 领域类不变式 + 持久层契约
      04..07-*.md        # 引擎与核心算法（含测试用例表）
      08-api.md          # 接口契约
      11-errors.md       # 错误码全表
      12-scheduler.md    # 调度/事务/幂等
      13-frontend.md     # 路由/权限/强制渲染
      14-test-plan.md    # 测试 + 自动检查 + 数据审计 + 验收回收
      15-tasks.md        # 里程碑 + 阻塞风险
    fig/                 # 图源码 + 渲染图
  scripts/check-*        # 源码红线自动检查
  evidence/*_audit + final_check   # 数据/运行态审计与终审
```

## 附录 B · 入口一页纸（工程约定文件）骨架

> 名称不限（团队约定即可），三区结构是本体。

```text
# <项目> 工程约定

## <主题> 红线（YYYY-MM-DD 起生效，所有新增必须满足）
- 红线表：记录类型 × 必备约束
- 落地方式：① 显式优先 ② 自动推导 ③ 冲突即报错（构造期，不静默）
- 存量状态：缺口数字 → 回填结果 → 终审结论 + 证据脚本路径

## 关键机制（实测结论，修改链路前必读）
- 每条：机制名 + 实测行为 + 正确写法 + 错误写法的后果
- （实例格式："X 是构造期硬解析：……直接抛错（不是软失败），因此回填必须先于模板切换"）

## 运维手册（实战补充）
- 重建恢复标准顺序：步骤 1 → 步骤 N → 终审（全绿标准逐条列出）
- 坑登记：<环境/工具> <现象> → <根因> → <必须这么做>
```
