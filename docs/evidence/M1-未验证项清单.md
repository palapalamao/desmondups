# M1 未验证项清单（段 3 收尾 · 方法论 v2.0 第 20 条：未验证项明示）

| # | 未验证项 | 原因（环境限制） | 替代验证已做 | 补验责任人 | 预计时点 |
|---|---|---|---|---|---|
| U-API-01 | upsOverview() Axon 函数（fan/UpsPodLib.fan）对 Folio/hisRead/alarm 的真实调用 | 无 FIN 5.3.0 实例 | 契约注释 + fail-closed 抛错占位 | Codex + 需求方（提供实例） | 目标实例就绪后 |
| U-UI-01 | 前端在 FIN 内的菜单/路由接线（U1 基类名未决） | 同上 | 详设 §6 设计意图 | Codex | U1 探测后 |
| U-UI-02 | ~~真机目检~~ → **已验证 2026-09-18**（kimi-webbridge 驱动真实浏览器） | — | Node 逻辑 8/8 + 真实浏览器走查 7/7 通过 | 已完成 | 已闭环 |
| U-UI-03 | R1 权限隔离（A7：站点级数据隔离） | Mock 适配器不做权限过滤（代码已注明） | 权限矩阵设计 + FinAdapter 桩注明由 FIN 会话驱动 | Codex（有实例后） | U-API-01 同期 |
| U-DATA-01 | xeto 语义库编译（lib/ups/ups-core.xeto） | 无 FIN 工具链 | FIN Expert 离线校验（通道不支持 xeto，见文件头） | Codex | 首次 fan build |
| U-INT-01 | 200 单元容量 / 首屏 ≤3s（详设 §5） | 无真实数据通道 | —（压测需实例） | Codex | 实例就绪后 |

## 真实浏览器走查记录（2026-09-18，kimi-webbridge，证据截图 docs/evidence/M1-*.png）

| 用例 | 结果 | 证据 |
|---|---|---|
| TC-M1-UI-01 总览渲染（60台/徽标/卡片/分页） | 通过 | M1-总览首页.png + wb-snap-overview.json |
| TC-M1-UI-03 站点筛选（A站→20台，hash 带参） | 通过 | M1-筛选A站.png |
| TC-M1-UI-05 点卡进详情（#/ups/unit/UPS-A52） | 通过 | 同上（hash 断言） |
| TC-M1-UI-06 返回后筛选状态保留（20台恢复） | 通过 | 同上 |
| TC-M1-UI-07 断讯卡虚线+通讯中断+缺口标注 | 通过 | M1-筛选A站.png（UPS A-37） |
| 走查中发现并修复 2 个真 bug | 见 CLAUDE.md 陷阱登记 #2/#3 | commit 见 spec 段3记录 |

## 验证已完成项（证据）

- TC-M1-逻辑-01~08：Node VM 纯逻辑验证 **8/8 通过**（命令：`node extensions/upsPod/test/verify.cjs`，输出见 git 历史/本文件版本记录）
- 种子确定性：重复生成逐字节一致（TC-M1-逻辑-08 内含断言）
- 红线落实核查：R3 时效三态在 UI 与逻辑层均有体现；R2 M1 无写操作（plan 活体数据操作组 = 无）

## 走查指引（补验 U-UI-02 用）

```bash
cd extensions/upsPod/frontend
npx serve .        # 或 python -m http.server 8080
# 浏览器打开 http://localhost:3000/#/ups/overview
```

要点：断讯卡虚线置灰（Q5）；"参考值/缺口"标注（R3）；翻页后点卡片→返回，筛选与页码保留（REQ-M1-07）。

## 真实浏览器走查记录 · v0.3 追加（2026-09-19，kimi-webbridge，证据 M1-v03-趋势总览.png）

| 用例 | 结果 | 证据/说明 |
|---|---|---|
| TC-M1-逻辑-09~11 | Node 纯逻辑 11/11 通过（含回归 01~08） | node verify.cjs 输出 11/11 |
| TC-M1-UI-08 双 sparkline 渲染 | 通过：第 1 页 49 活卡 × 2 SVG = 98，0 空 polyline，24 点/线 | evaluate 断言 |
| TC-M1-UI-09 断讯/缺口占位（Q11） | 通过：第 2 页 4 断讯卡"趋势不可用 · 通讯中断"+ 1 缺口卡"无数据"，等高虚线不跳动 | evaluate + 截图 |
| TC-M1-UI-10 趋势滚动（跨轮询） | 通过：隐藏页节流环境下等 65s，points 变化且窗长恒定 24（rollHistory shift 生效） | evaluate 前后对比 |
| TC-M1-UI-11 筛选回归 | 通过：site-a → 20 卡（18 活 + 2 占位），趋势区不受影响 | evaluate 断言 |
| TC-M1-UI-12 路由回归 | 通过：#/ups/unit/UPS-A01 占位 + 返回按钮正常 | evaluate 断言 |
| 顺带修复 latent bug | tsOffsetSec→ts 契约此前未接线，首屏全部误显"缺口"；按种子注释补接线后 49 ok / 1 gap / 4 stale 符合种子设计 | 本段代码 + 走查 |

> 登记日期：2026-09-18（v0.2 基线）/ 2026-09-19（v0.3 追加）· 对应 spec：docs/specs/M1-设备总览.md 段 3

## 真实浏览器走查记录 · M2 v0.4 追加（2026-09-19，kimi-webbridge）

| 用例 | 结果 | 证据/说明 |
|---|---|---|
| TC-M2-UI-01 详情 8 面板渲染（头部档案区+7 面板；参考值徽标 ×4：预估备电/预测更换/模型备电/风险项） | 通过 | M2-详情页-UPS-A01.png + evaluate 断言（panels=7，refBadges=4） |
| TC-M2-UI-02 枚举中文映射（拓扑 在线双变换 / 供电模式 市电·电池·旁路·未知，按 tags.xeto） | 通过 | archive-bar 文本断言 |
| TC-M2-UI-03 告警筛选交互（点未确认→仅 1 条未确认可见；回全部→2 条） | 通过 | click + evaluate 断言（Ractive 坑2：标量参数正常传递） |
| TC-M2-UI-04 告警区零写入口（按钮仅 全部/未确认/已确认 筛选；DOM 无确认按钮） | 通过 | evaluate 断言 + r2 检查扩展（unit 模板段禁 ack 处理器，详设 §5） |
| TC-M2-UI-05 时效三态（ok=实测 / stale=参考值(UPS-C06 偏移198s 首窗) / gap=缺口(断讯)） | 通过 | evaluate 断言；轮询后自动回实测，证明 detail.ts 跟随修复生效 |
| TC-M2-UI-06 断讯单元 detail=null（D-M2-02：档案区全保留、遥测 0 面板、整页 fatal 明示、预估备电隐去） | 通过 | M2-断讯页-UPS-A07.png + evaluate 断言 |
| TC-M2-UI-07 未知 id fail-closed（#/ups/unit/UPS-Z99 → 未找到设备，不兜底） | 通过 | evaluate 断言 |
| TC-M2-UI-08 返回上下文保留（A13 回归：site/sev/page hash 恢复，筛选态还原） | 通过 | 卡片点击→history.back()→evaluate 断言 |
| 走查中发现并修复 2 项 | ①detail.ts 首次派生后不跟随 poll 刷新的 unit.ts（隐藏页节流放大暴露：首屏 3 分钟后组时效误显参考值）→ 每次读派生；②拓扑/模式原文直出 → 按 tags.xeto 枚举映射中文（枚举先行纪律） | 本提交代码 + 走查 |

> TC-M2-01 措辞澄清（对 spec 段 2）：断讯/缺口单元 detail=null，档案区（名称/站点/位置/拓扑/厂商/型号/序列号）照常渲染，遥测面板整区不渲染（D-M2-02 口径）。
> M2 未闭环 U-* 项：U-API-01 / U-UI-01 / U-UI-03 / U-DATA-01 / U-INT-01 不变（均待 FIN 5.3.0 实例），本清单继续有效。

## 真实浏览器走查记录 · M3 v0.5 追加（2026-09-19，kimi-webbridge）

| 用例 | 结果 | 证据/说明 |
|---|---|---|
| TC-M3-UI-01 24h 三指标双区图（上区 SOC+负载共轴/下区温度独立轴/5 档 x 刻度/图例含缺口留白） | 通过 | M3-历史页-24h.png + evaluate 断言（polylines=3，ticks=5，grain=5min） |
| TC-M3-UI-02 30d 缺口分段 + 事件配对（UPS-C03：soc/load 各断 1 次 → 4 段；3×inputFail+3×transferOk 与 Node 预测一致） | 通过 | M3-历史页-30d缺口与事件.png + evaluate 断言 |
| TC-M3-UI-03 跨度/指标按钮交互（hash 驱动深链可复现；粒度随跨度切换 10s→2h） | 通过 | click + evaluate 断言 |
| TC-M3-UI-04 显式空指标 → 明示提示不静默回弹（走查修复：normalize 缺省/显式空分流） | 通过 | navigate metrics= → fatal 提示 + 0 曲线 |
| TC-M3-UI-05 断讯机历史可查（D-M3-03：banner 明示"历史为已入库数据"，曲线正常渲染） | 通过 | UPS-A07 7d：polylines=4，grain=30min |
| TC-M3-UI-06 M2 入口链接（电池/负载/环境三面板"全量历史 →"，带指标上下文页内跳转） | 通过 | click .hist-link → view=history + metrics 正确 |
| TC-M3-UI-07 返回链路（历史→返回按钮→总览/详情上下文恢复） | 通过 | click goBack（A17） |
| TC-M3-UI-08 非法参数 fail-closed 收敛（bad span→24h 激活档明示；bad metrics→空+提示；生效值标注 D-M3-07） | 通过 | navigate 非法参数 + evaluate 断言 |
| 走查中发现并修复 1 项 + 登记 1 坑 | 修复：normalizeHistoryQuery 显式空保持空（原实现静默回弹默认，违反 fail-closed 明示精神）；坑：webbridge evaluate 改 hash/history.back 为隔离世界不触发页面 hashchange——走查一律用页内 click | 本提交代码 + CLAUDE.md 坑登记 |

> M3 未闭环 U-* 项不变（U-API-01：FinAdapter.history 桩待实例解锁）。
## 真实浏览器走查记录 · M4 v0.6 追加（2026-09-19，kimi-webbridge，会话 m2-v04）

| 用例 | 结果 | 证据/说明 |
|---|---|---|
| TC-M4-UI-01 告警列表渲染（59 行=种子全量拍平；40 未确认带确认按钮；徽标 活动40·已确认19·严重14） | 通过 | M4-01-告警列表.png + evaluate 断言 |
| TC-M4-UI-02 行内确认展开（点确认→该行展开 textarea+提交/取消；同时只一行） | 通过 | click + evaluate 断言（ackOpen=UPS-C06#0，editor=1） |
| TC-M4-UI-03 守门序3 拦截（备注"x"<2字→提交被拒，明示"备注必填（至少 2 字）"，编辑区保留可改） | 通过 | click 提交 + evaluate 断言（ackError 非空，editorStill=1） |
| TC-M4-UI-04 成功确认（合法备注→行变"已确认 · mock-operator · 2026/9/19 16:14:36"，by/at 取自审计事件；活动徽标 40→39） | 通过 | evaluate 断言（rows 数据 ackInfo 全格式） |
| TC-M4-UI-05 种子已确认行 fail-closed 披露（无审计事件→"已确认（审计缺失，fail-closed 明示）"，不编造 by/at） | 通过 | evaluate 断言 ackedSamples |
| TC-M4-UI-06 确认后按钮消失（UI 单向 A21：已确认行无操作按钮，重开页后幂等） | 通过 | M4-02-已确认行无按钮.png + evaluate（noButtonOnAcked=true） |
| TC-M4-UI-07 编辑区跨轮询存活（14s≥1 轮询周期：ackOpen/ackNote/edtior 全保持；修复实证：原 poll 重渲染清空编辑中输入） | 通过 | 3/5/6s 间隔 evaluate 断言 + renderAlarms 保留逻辑 |
| TC-M4-UI-08 取消按钮（编辑区关闭，ackOpen=null） | 通过 | click + evaluate 断言 |
| TC-M4-UI-09 深链筛选（?state=unack&sev=critical→14 行全严重全未确认；筛选值回填 select） | 通过 | navigate + evaluate 断言（与徽标严重14一致） |
| TC-M4-UI-10 M2 衔接（详情页告警面板"去处理 →"→#/ups/alarms?unit=UPS-C06→1 行；R2 详情区仍只读） | 通过 | click + evaluate 断言 |
| TC-M4-UI-11 徽标点击筛选（点活动徽标→hash ?state=unack→39 行，hash 驱动可复现） | 通过 | click + evaluate 断言 |

> 走查中发现并修复 1 项：poll 重渲染 reset 清空编辑中 ackOpen/ackNote（用户输入每 10s 丢失）→ renderAlarms 重渲染保留编辑态（行仍存在且未确认才保留）。同会话内因用户并行操作浏览器致 1 次中间态跳变（view=unit），与产品代码无关，重跑通过。
> M4 未闭环 U-* 项不变（U-API-01：FinAdapter ack 通道桩待 FIN 5.3.0 实例解锁；R1 站点授权 Mock 全员=参数留位）。