# M6 审计 · 设计决策记录（spec）

| 项 | 内容 |
|---|---|
| 日期 / 分支 | 2026-09-19 / main |
| 当前阶段 | 段 3 开发（本提交）/ 段 1 ✓（e73dd79）/ 段 2 ✓（b642405） |
| 上游需求 | 业务方案 v0.8 M6 三段式、验收 A26~A28、铁律 4、R2·R3 |
| 视觉基线 | 原型列表语言（M4 告警页同构）扩展为审计查询页 |

## Goal

M6 审计：审计流的**只读查询视图**（SPA 路由 `#/ups/audit`）。消费方模块——零新发射源，消费 M4（alarm.ack）/M5（config.publish/config.rollback）已发射的审计事件。

- REQ-M6-01 审计查询视图：全量事件列表（seq/时间/类型/操作者/摘要），seq 倒序（新的在前）
- REQ-M6-02 筛选：类型/操作者/时间档/关键字；深链参数化 `#/ups/audit?type=&by=&since=&q=`
- REQ-M6-03 计数徽标：按类型计数，激活高亮（M1/M4 语言延续）
- REQ-M6-04 行内展开：完整字段（备注/告警消息/变更摘要）；同时只展开一行
- REQ-M6-05 append-only：视图零写入口——不可改/删/补录（铁律 4，A27 CI 可证）
- REQ-M6-06 空态明示：无记录/筛选空 → 明示不造假（R3）
- REQ-M6-07 入口衔接：总览头部"审计 →"；Mock 演示语义（重载清空审计流）UI 明示
- REQ-M6-08 裁剪留痕：导出 CSV/定期归档/FIN 内核审计对接 = 二期（U-API-01）

## 决策点（待需求方确认，建议已给）

| # | 决策点 | 建议 | 备选 |
|---|---|---|---|
| Q1 | 数据源 | 仅运行时审计流（M4/M5 发射）；FIN 内核审计表 = U-API-01 后扩展 | 自建持久化（否决：Mock 环境造持久层=过度设计） |
| Q2 | 时间筛选 | 相对档（全部/1h/24h/7d），锚点注入可测 | 绝对区间选择器（二期） |
| Q3 | 操作者筛选 | 动态收集下拉（从事件 by 字段去重） | 自由文本输入（备选保留） |
| Q4 | 内存流丢失 | 明示"演示环境：重载清空"（R3 宁缺毋假，不装持久） | localStorage 伪持久（否决：破坏确定性重建叙事，M5 同规） |
| Q5 | 类型枚举 | 三类钉死：alarm.ack/config.publish/config.rollback（中文映射标签色） | 类型自由扩展（否决：元数据失锚，筛选无法枚举） |
| Q6 | 导出 | 不做（二期） | 导出 CSV（二期：有持久层后才有意义） |

## 模型缺口与对策（段 2 落地）

- 零新增：纯查询视图；审计事件结构 M4/M5 已契约化（段 2 只定查询函数与类型元数据）
- r2 检查扩展：audit 模板段禁写 handler 扫描（A27 CI 可证）

## 接口预期（段 1 初判）

零新增：FinAdapter 侧读 FIN 审计内核表（U-API-01 解锁）；Mock 侧消费内存 auditLog。

## 版本

0.8.0 同批联动（build.fan/app.js/seedVersion）——段 3 执行。

## 阶段闸门

- 段 1：业务方案 v0.8 + spec + plan → 提交 → 停下等确认
- 段 2：详设（查询契约/类型元数据/r2 扩展/测试锚点）+ plan → 提交 → 停下等确认
- 段 3：代码 + 测试 + 版本联动 + 走查 + 证据 → 提交收尾
- 不得越段动工。

## 段 N 记录（执行后回写）

- 段 1：本提交（业务方案 v0.8 + spec/plan 初稿，Q1~Q6 建议稿）
- 段 2 回写：
  - 详设：docs/02-详细设计/M6-审计-详设.md（queryAudit 契约/类型元数据/r2 audit 段扩展/测试锚点 01~05）
  - r2 检查扩展：audit 模板段禁写 handler 扫描（deleteAudit/editAudit/modifyAudit/updateAudit/removeAudit/clearAudit/purgeLog，A27 CI 可证）
- 段 3：本提交（queryAudit/collectOperators/auditDetailFields/AUDIT_TYPES 落地 + audit 视图/徽标/展开/筛选；TC-M6-01~05 全过 35/35；走查 5 项全过：空态明示/端到端发布事件可查/行展开明细/徽标类型筛选/关键字深链）
