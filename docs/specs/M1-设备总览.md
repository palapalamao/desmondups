# M1 设备总览 · 设计决策记录（spec）

| 项 | 内容 |
|---|---|
| 日期 / 分支 | 2026-09-18 / feat/M1-fleet-overview |
| 当前阶段 | v0.3 段 2 设计（已提交，待确认）/ v0.3 段 1 ✓（3a9cd97）/ v0.2 段 1~3 ✓ |
| 上游需求 | docs/01-业务方案 v0.2 REQ-M1-01 ~ REQ-M1-09、验收 A6~A8 |

## Goal

M1 设备总览：授权范围内全部 UPS 单元的一屏式健康总览（SPA 单页，路由 #/ups/overview）。
子需求：

- REQ-M1-01 列表字段：站点/名称/位置/通讯状态/供电模式/负载%/SOC/最高严重度
- REQ-M1-02 四维筛选：站点/通讯/模式/严重度
- REQ-M1-03 严重度分类统计徽标
- REQ-M1-04 分页 50/100
- REQ-M1-05 每项采集时间标注，超时显式标缺口/参考值（红线 R3）
- REQ-M1-06 新严重风险提示（不自动重排，见 Q6）
- REQ-M1-07 点击行进 M2，返回保留筛选/页码
- REQ-M1-08 深链 #/ups/overview（D-06：FIN 顶栏菜单传路由）
- REQ-M1-09 权限隔离：仅见授权站点（红线 R1，验收 A7）- REQ-M1-10 卡片内嵌历史趋势：SOC（主）+ 负载（次）双 sparkline，视觉基线 docs/demo 原型（验收 A9）

## 已确认决策

| # | 决策点 | 结论 | 备选（已否决 / 二期） |
|---|---|---|---|
| Q1 | 总览形态 | 卡片网格（沿用 docs/demo 工业仪表盘视觉）——已确认 2026-09-18 | 表格紧凑视图（二期可加） |
| Q2 | 实时刷新 | 轮询 10s 可配（推送通道二期，待 U1~U5 探测后评估）——已确认 2026-09-18 | WS/SSE 推送（二期，探测后决策） |
| Q3 | 严重度来源 | 复用 FIN alarm 内核 severity（零新增优先）——已确认 2026-09-18 | 自建分级体系（否决倾向：违反零新增） |
| Q4 | 默认排序 | 严重度 > 通讯状态 > 名称——已确认 2026-09-18 | 按站点分组（备选） |
| Q5 | 断讯设备的展示 | 保留原位 + 整行标通讯中断态，不隐藏（对应 F4）——已确认 2026-09-18 | 自动沉底/隐藏（否决倾向：掩盖异常） |
| Q6 | 新严重风险提醒 | 徽标+横幅提示，手动刷新排序——已确认 2026-09-18 | 实时重排列表（否决倾向：行跳动易误点） |
| Q7 | 负载/SOC 取数 | hisRead 最近值+采集时间戳，无值标缺口——已确认 2026-09-18 | 规则引擎聚合（二期） |

> Q1~Q7 于 2026-09-18、Q8~Q11（新需求：卡片趋势）于 2026-09-19 由需求方按建议确认；备选栏保留作否决留痕。
> 注：Q8~Q11 为 v0.3 追加决策，走查基线 = 原型 sparkline 设计（SVG polyline + 固定量程 + 滚动缓冲）。

## 模型缺口与对策

已落地（段 2）：
- lib/ups/ups-core.xeto：upsSite / upsUnit 最小集（M2 电池串/模块等段 2 再扩）
- 最高严重度聚合：FIN alarm 内核 severity（Q3 已确认），落点 = Axon 聚合函数 upsOverview()（详设 §2）
- 结构红线首条：upsUnit.site 必填（推导链与冲突策略在阶段 2 红线表成文）

## 接口预期

优先结论 = 零新增：读数走既有 hisRead/点位读通道，告警走既有 alarm 体系。
段 2 论证；若探测发现 U4/U5（alarm ack / hisRead 语义）不满足，新增须论证。

## 版本

段 3 定；预计联动：ext pod 版本、业务方案 v0.2→v0.3、spec/plan 版本行。

## 阶段闸门

- 段 1：需求文档 + 本 spec → 提交 → **停下等确认（当前在此）**
- 段 2：详设 + 模型 + 接线（FIN 菜单/词条）+ plan → 提交 → 停下等确认
- 段 3：代码 + 测试 + 版本联动 + 演示数据 + 真机目检 + 证据 → 提交收尾
- 不得越段动工。

## 段 N 记录（执行后回写）

- 段 1：755b978（业务方案 v0.2 + spec/plan 初稿，Q1~Q7 待确认状态提交）
- v0.3 段1：3a9cd97（REQ-M1-10 需求 + Q8~Q11 确认，业务方案 v0.3 + 验收 A9）
- v0.3 段2：待回填（详设 §7 趋势设计 + D-M1-12~15，提交后回填 hash）





