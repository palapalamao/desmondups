# 业务方案（草案 v0.1）— desmondUPS UPS 车队监控 POD

> 阶段 1 产出草稿。本文件不覆盖技术详设（属 docs/02-详细设计）。
> 状态：骨架 + 待补清单，未过门禁，不可作为签字版。

| 项 | 内容 |
|---|---|
| 版本 | v0.1 草案 |
| 日期 | 2026-09-18 |
| 基线 | UI prototype/AI-CES-IoT-UPS（Ractive 原型，视觉/交互基线） |

## 0. 术语钉死（先于一切设计）

| 术语 | 本项目中含义 | 排除含义 |
|---|---|---|
| POD | FIN Framework 5.3.0 上的 UPS 车队监控扩展模块（ext pod） | SAP SD 的 Proof of Delivery；K8s Pod；SAP DM 的 Production Operator Dashboard |
| SAP | 待澄清：客户侧上位系统代号 or 笔误 | — |

> "SAP"的确切含义需需求方确认后回填；未确认前所有文档不出现该词，只写 "UPS 车队监控 POD"。

## 1. 领域结构性差异（UPS 车队 vs 商业建筑楼宇自控）——待补

- [ ] 供电连续性 = 生命级硬约束（断电即事故），区别于舒适型楼宇负载
- [ ] 设备异构：在线双变换 /  line-interactive / 模块化 N+1，能力矩阵差异大
- [ ] 关键指标口径：续航时间、SOC/SOH 基准、严重度分级——均需口径确认单
- [ ] 远程站点分散，通讯中断是常态而非异常
- [ ] （每条差异须推导出一个设计结论——阶段 1 门禁）

## 2. 失败模式清单（初稿）

| # | 失败模式 | 规避设计方向 |
|---|---|---|
| F1 | 平台展示数据与 UPS 实际状态不一致 → 运维误判 → 断电事故 | 数据时效显式标注；过期即标"参考值"；阈值可配 |
| F2 | 配置误下发（电池参数、阈值）→ 设备保护动作或损坏 | 守门引擎 fail-closed；下发前校验 + 版本化 + 全量留痕 |
| F3 | 告警风暴/重复确认 → 运维麻木，真告警被淹没 | 告警确认幂等；活动/已确认状态机清晰 |
| F4 | 通讯中断被当成"设备正常" | 通讯状态独立建模，缺口显式表达 |
| F5 | 口径争议（"续航怎么算"）导致指标不被信任 | 口径确认单多方会签，未会签标参考值 |

## 3. 设计铁律（草案，≤5 条，待决策层认可）

1. 保障优先：任何与供电安全冲突的功能让路。
2. 数据诚实：宁缺毋假，缺口与参考值显式标注。
3. 写操作守门：fail-closed，无证据即拒绝，否决留痕。
4. 历史不可改：版本化 + 红冲 + 幂等。
5. 只读优先集成：默认不写控制点。

## 4. 角色与功能模块（编号即需求主键）——骨架，待补三段式

| 模块 | 名称 | 原型对应 | 状态 |
|---|---|---|---|
| M1 | 设备总览 | 设备 tab/筛选/统计 | 三段式详述见下（v0.2 段1） |
| M2 | 设备详情 | 通讯/供电/电池串/模块/告警 | 待三段式详述 |
| M3 | 历史趋势 | SOC/负载/温度历史 | 待三段式详述 |
| M4 | 告警与确认 | 告警列表/确认/备注 | 待三段式详述 |
| M5 | 配置管理 | 注册/能力/映射/校验/发布 | 待三段式详述 |
| M6 | 审计 | 操作留痕/查询 | 待三段式详述 |

> 三段式：使用者 / 解决的问题 / 核心功能。每个模块补齐后方可过阶段 1 门禁。

## 5. 量化验收标准（初稿，交付时逐条回收）

| # | 标准 | 目标值 | 验证方式 |
|---|---|---|---|
| A1 | 实时数据时效 | ≤ 10s（或按采集周期显式标注） | 现场实测 |
| A2 | 告警确认留痕完整率 | 100% | 审计记录抽查 |
| A3 | 无效配置下发拦截率 | 100%（校验未过不可发布） | 测试用例 TC-M5-* |
| A4 | 数据缺口显式标注率 | 100%，禁止静默兜底 | UI 走查 + 接口断言 |
| A5 | 任意历史账期/配置版本可重放 | 100% | 用旧版本实测 |

## 6. 阶段 1 门禁自查

- [ ] 差异表每条差异有设计结论
- [ ] 失败模式每条有规避设计
- [ ] 铁律 ≤5 条且决策层认可
- [ ] 模块三段式完成并编号
- [ ] 验收标准双方书面确认
- [ ] "SAP"术语澄清回填

## 6.5 已确认决策表（v2.0 新增：否决项留痕，防止重开已关闭的争论）

| # | 决策点 | 结论 | 备选（已否决 / 二期） |
|---|---|---|---|
| D-01 | 技术底座 | FIN Framework 5.3.0，交付物为 ext pod | 否决：SAP DM 的 Production Operator Dashboard 方向；否决：纯静态页面交付（不可验收） |
| D-02 | "POD" 术语 | = FIN 上的 UPS 车队监控扩展模块 | 否决：SAP SD 的 Proof of Delivery；否决：K8s Pod（与本项目无关） |
| D-03 | 视觉/交互基线 | AI-CES-IoT-UPS Ractive 原型（移入 docs/demo/ 作演示先行材料） | 否决：UPS-FSD-R2 静态原型（用户已删除，不进基线） |
| D-04 | 原型代码去留 | 原型仅作演示基线，不演进为生产代码；生产前端在 extensions/ 重建 | 否决：直接改造原型文件为生产代码（演示与交付物必须分离） |
| D-05 | "SAP" 一词 | 暂停使用，待需求方澄清含义后回填 | 悬而未决——非否决，属未决项 |



### M1 设备总览 · 三段式详述（v0.2，微流程段 1 产出）

- **使用者**：运维值班员、车队管理员（经 FIN 授权的用户，按站点隔离可见范围）
- **解决的问题**：一屏掌握全部 UPS 单元的健康与供电状态，快速定位需要处置的设备；
  杜绝"列表看着正常、实际已断讯/告警"的误判（对应失败模式 F1/F4）
- **核心功能**：
  1. 授权范围内全部 UPS 单元列表：站点 / 名称 / 位置 / 通讯状态 / 供电模式 / 负载% / 电池 SOC / 当前最高严重度
  2. 筛选：站点、通讯状态、供电模式、严重度
  3. 分类统计徽标：按严重度计数（严重/警告/正常/断讯）
  4. 分页：50/100 每页
  5. 数据时效标注：每项附采集时间；超过采集周期未更新显式标注（缺口/参考值，红线 R3）
  6. 新严重风险提示：列表中出现新的严重级设备时给出可见提示（不自动刷新排序造成跳动——见 spec 决策 Q6）
  7. 点击行进入 M2 设备详情，返回时保留筛选与页码上下文
  8. 深链直达：`#/ups/overview`（SPA 路由，FIN 顶栏菜单传参）

**M1 量化验收标准（追加到 A 组）**

| # | 标准 | 目标值 | 验证方式 |
|---|---|---|---|
| A6 | 总览数据时效 | ≤ 10s（可配置），超时项显式标注 | 现场实测 + UI 断言 |
| A7 | 权限隔离 | 用户仅见授权站点数据，100% 无越权 | 权限矩阵测试 TC-M1-* |
| A8 | 筛选/分页/返回上下文 | 全组合可用，返回后状态保持 | UI 走查 || A9 | 卡片趋势：断讯卡不渲染趋势（防误导）；趋势随轮询滚动更新 | 100% 卡片一致 | UI 走查 + 逻辑断言 |

### 已确认决策表（追加）

| # | 决策点 | 结论 | 备选（已否决 / 二期） |
|---|---|---|---|
| D-06 | "SAP" 澄清 | **SPA = Single Page Application**（单页应用）：单一 index.html，# 号路由（如 #/ups/overview），FIN 顶栏菜单传路由给单页 | 否决：SAP 公司软件相关含义全部排除；原词"SAP"系 SPA 笔误 |

