# 决策追认表 — Q1~Q6（M1~M6）

> 方法论 v2.0：「已确认决策表，否决项留痕，防止重开已关闭的争论」
> 登记日期 2026-09-19
> **状态说明**：M1/M2 为需求方当面确认（2026-09-18/19）；M3~M6 为"建议稿已按既定方向执行完毕"，本表供需求方**追认签字**。各模块明细以 docs/specs/ 对应文件为源，本表为汇总视图。

## M1 设备总览（已确认 2026-09-18/19）

| # | 决策点 | 结论 | 备选（已否决/二期） |
|---|---|---|---|
| Q1 | 总览形态 | 卡片网格（沿用原型工业仪表盘视觉） | 表格紧凑视图（二期可加） |
| Q2 | 实时刷新 | 轮询 10s 可配 | WS/SSE 推送（二期，U1~U5 探测后评估） |
| Q3 | 严重度来源 | 复用 FIN alarm 内核 severity（零新增优先） | 自建分级体系（否决：违反零新增） |
| Q4 | 默认排序 | 严重度 > 通讯状态 > 名称 | 按站点分组（备选） |
| Q5 | 断讯设备展示 | 保留原位 + 整行标通讯中断态，不隐藏 | 自动沉底/隐藏（否决：掩盖异常） |
| Q6 | 新严重风险提醒 | 徽标+横幅提示，手动刷新排序 | 实时重排列表（否决：行跳动易误点） |
| Q7 | 负载/SOC 取数 | hisRead 最近值+采集时间戳，无值标缺口 | 规则引擎聚合（二期） |
| Q8~Q11 | 卡片内嵌趋势（v0.3 追加） | 双 sparkline（负载/SOC）+ 24 点滚动窗 0–100 + 断讯等高占位 | 单图合并/动态量程/断讯隐藏（均否决，详见 spec） |

## M2 设备详情（已确认 2026-09-19）

| # | 决策点 | 结论 | 备选 |
|---|---|---|---|
| Q1 | 面板取舍 | 8 面板全保留；无传感器项按 R3 缺口标注 | 裁剪环境面板（否决：掩盖能力缺口） |
| Q2 | 健康环 healthScore | **裁剪**；以"最高严重度+未确认告警数"替代头部呈现 | 派生算法（二期） |
| Q3 | 预测性维护 | 保留展示+参考值口径+来源；预测算法二期 | 整体裁剪（否决：原型核心卖点） |
| Q4 | 详情内嵌趋势 | 与 M1 同规（24 点滚动窗 0–100）；全量历史归 M3 | 详情页加粒度切换（否决：M3 职责越界） |
| Q5 | 告警列表范围 | 全部+确认状态筛选+未确认优先 | 只显示未确认（备选） |

## M3 历史趋势（按建议稿执行，待追认 ☑）

| # | 决策点 | 结论 | 备选 |
|---|---|---|---|
| Q1 | 指标范围 | 三指标多选（默认 SOC+负载），单图多折线共轴 0-100 定程，温度独立 | 全指标固定同屏（否决：温度量程压扁曲线） |
| Q2 | 跨度/粒度 | 预设四档 + 粒度自适应（1h→原始 / 24h→5min / 7d→30min / 30d→2h） | 自由起止+手选粒度（二期） |
| Q3 | 事件叠加 | 叠加市电中断/切换成功事件标记+图例 | 不叠加（否决：事故复盘锚点丢失） |
| Q4 | 入口边界 | 仅 M2 详情进入（M1 卡片不进，D-07 不破） | M1 卡片加"历史"按钮（否决：违反 D-07） |
| Q5 | 多单元对比 | 不做，M3 单单元 | 同图多单元（二期） |
| Q6 | 断讯段处理 | 留白+缺口图例，禁插值（R3） | 虚线连接最后已知值（否决：给过期数据续命） |

## M4 告警与确认（按建议稿执行，待追认 ☑）

| # | 决策点 | 结论 | 备选 |
|---|---|---|---|
| Q1 | 确认权限 | Mock 全员可确认；站点授权走 ctx 参数（R1 留位） | Mock 分角色（否决：假角色无意义） |
| Q2 | 备注 | 必填，最短 2 字（留痕质量 A2） | 选填（否决：留痕空洞化） |
| Q3 | 批量确认 | 不做（单条，防误操作） | 批量（二期） |
| Q4 | 确认撤销 | 不可撤销（历史不可改，铁律 4） | 可撤销（否决：破坏审计链） |
| Q5 | 列表范围 | 授权内全车队 + 筛选 | 仅当前站点（否决：值班员跨站处置是常态） |
| Q6 | 恶化复发 | 出新告警 = 新记录（不复用已确认行） | 重开旧行（否决：破坏状态机单向） |

## M5 配置管理（按建议稿执行，待追认 ☑）

| # | 决策点 | 结论 | 备选 |
|---|---|---|---|
| Q1 | 配置范围 | MVP=告警阈值（SOC 下限/内阻基线偏差%/温度上限），档案/点位映射只读 | 全量可编（否决：范围爆炸） |
| Q2 | 编辑方式 | 草稿+发布两段式（草稿不碰生效值，A25） | 直接生效（否决：误改即事故） |
| Q3 | 校验时机 | 发布前守门 fail-closed（A3） | 输入时即时校验（备选保留：UI 即时提示仍在） |
| Q4 | 版本化粒度 | 每次发布=全量快照版本（seq 递增，简单可重放） | 差量版本（否决：重放复杂，审计链断裂风险） |
| Q5 | 回滚语义 | 红冲：以旧版内容生成新版本（历史零修改，铁律 4） | 物理恢复旧版（否决：篡改历史） |
| Q6 | 审批流 | 不做（单操作者直发+审计，A24 兜底） | 双人审批（二期：有 FIN 会话后接角色） |

## M6 审计（按建议稿执行，待追认 ☑）

| # | 决策点 | 结论 | 备选 |
|---|---|---|---|
| Q1 | 数据源 | 仅运行时审计流（M4/M5 发射）；FIN 内核审计表 = U-API-01 后扩展 | 自建持久化（否决：Mock 环境造持久层=过度设计） |
| Q2 | 时间筛选 | 相对档（全部/1h/24h/7d），锚点注入可测 | 绝对区间选择器（二期） |
| Q3 | 操作者筛选 | 动态收集下拉（事件 by 字段去重） | 自由文本输入（备选保留） |
| Q4 | 内存流丢失 | 明示"演示环境：重载清空"（R3 宁缺毋假） | localStorage 伪持久（否决：破坏确定性重建） |
| Q5 | 类型枚举 | 三类钉死：alarm.ack / config.publish / config.rollback（中文映射+标签色） | 类型自由扩展（否决：元数据失锚） |
| Q6 | 导出 | 不做（二期） | 导出 CSV（二期：有持久层后才有意义） |

## 追认签字

| 模块 | 决策人 | 签字/日期 |
|---|---|---|
| M3 历史趋势 | | |
| M4 告警与确认 | | |
| M5 配置管理 | | |
| M6 审计 | | |
