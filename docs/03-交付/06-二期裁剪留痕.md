# 二期裁剪留痕 — 已裁剪功能清单

> 方法论 v2.0：裁剪必须**显式留痕**，禁止静默丢弃（验收 A10 的依据文件）。
> 登记日期 2026-09-19 · 每项含：裁剪理由 + 二期触发条件。需需求方确认"本期不实现、二期再议"。

## M1 设备总览

| 裁剪项 | 理由 | 二期触发条件 |
|---|---|---|
| 表格紧凑视图 | 卡片网格已定（Q1），表格为备选 | 大屏值班场景提出密度诉求 |
| WS/SSE 实时推送 | 轮询 10s 已满足时效（A1）；推送需 U1~U5 探测 | FIN 实例探测后评估 |
| 规则引擎聚合（负载/SOC） | hisRead 最近值口径已够（Q7） | 出现跨点位聚合需求 |

## M2 设备详情

| 裁剪项 | 理由 | 二期触发条件 |
|---|---|---|
| 健康环 healthScore 派生算法 | 以"最高严重度+未确认告警数"替代已够（Q2） | 客户对健康度有明确算法要求 |
| 预测性维护预测算法 | 保留展示+参考值口径，算法本身不实装（Q3） | 有训练数据与算法资源 |
| 详情页粒度切换 | 全量历史归 M3，职责边界 D-07（Q4） | 用户反馈详情内切换诉求强烈 |

## M3 历史趋势

| 裁剪项 | 理由 | 二期触发条件 |
|---|---|---|
| 多单元同图对比 | M3 单单元收住范围（Q5） | 横向对比成为日常复盘刚需 |
| 自由起止+手选粒度 | 预设四档+自适应已覆盖（Q2） | 审计/取证类精确区间需求 |
| 历史导出 | 无持久层时导出意义有限 | 接 FIN 持久层后 |

## M4 告警与确认

| 裁剪项 | 理由 | 二期触发条件 |
|---|---|---|
| 批量确认 | 单条防误操作（Q3） | 告警量大且有审批兜底 |
| 确认撤销 | 违反状态机单向与审计链（Q4） | 原则上永不放开；如放开需走新版本记录 |
| 告警规则配置 | 属规则引擎范畴，超出 MVP | 有规则管理需求单独立项 |

## M5 配置管理

| 裁剪项 | 理由 | 二期触发条件 |
|---|---|---|
| 双人审批流 | 单操作者+审计留痕兜底（Q6） | FIN 会话角色体系可用 |
| 批量下发 | 发布=全量快照，批量语义复杂 | 多站点同配诉求 |
| 真实 pointWrite 下发 | Mock 语义已明示；写通道待 U-API-01 | FIN 实例就绪（此项即 U-API-01 的一部分） |

## M6 审计

| 裁剪项 | 理由 | 二期触发条件 |
|---|---|---|
| 导出 CSV | 内存流无持久层（Q6） | 接 FIN 内核审计表后 |
| 定期归档 | 同上 | 同上 |
| FIN 内核审计表对接 | U-API-01 后扩展（Q1） | FIN 实例就绪 |
| 绝对时间区间筛选 | 相对档已覆盖日常（Q2） | 取证类需求 |

## 确认栏

| 角色 | 签字/日期 |
|---|---|
| 需求方决策人（确认本期裁剪、二期再议） | |
| 开发方 | Codex（已按上表执行）2026-09-19 |
