# 验收对账表 — A1~A28 逐条回收

> 方法论 v2.0 · 阶段 7「验收标准逐条对账回收」 · 登记日期 2026-09-19
> 验证环境：确定性种子 + Mock 适配器（无 FIN 5.3.0 实例）。
> 可复跑证据：`node extensions/upsPod/test/verify.cjs`（逻辑 35/35）、`node scripts/check-all.cjs`（红线 r1~r5 5/5）、kimi-webbridge 真实浏览器走查 23 项全过（截图 `docs/evidence/*.png`）。

**状态图例**：✅ Mock 环境已闭环（附证据） / ⏳ 待 FIN 5.3.0 实例（未闭环项编号见《03-配合事项与阻塞风险》）

| # | 验收标准（业务方案原文摘要） | 方案要求验证方式 | 实测结果 | 状态 |
|---|---|---|---|---|
| A1 | 实时数据时效 ≤10s（或按采集周期显式标注） | 现场实测 | 轮询周期 10s 可配、默认生效；断讯/缺口三态显式标注（R3）。真实通道时效属实例侧，登记 U-API-01 | ⏳ |
| A2 | 告警确认留痕完整率 100% | 审计记录抽查 | TC-M4 逻辑（applyAck 发射事件带操作者/时刻/备注）+ TC-M6-UI-02 端到端真实点击后审计可查 | ✅ |
| A3 | 无效配置下发拦截率 100%（校验未过不可发布） | TC-M5-* | TC-M5 逻辑（V1~V4 全量收集、fail-closed）+ TC-M5-UI-02 走查（生效值未动） | ✅ |
| A4 | 数据缺口显式标注率 100%，禁止静默兜底 | UI 走查 + 接口断言 | M1 v0.3 走查：4 断讯卡"趋势不可用·通讯中断"+ 1 缺口卡"无数据"等高占位；M3 走查：缺口段留白+图例 | ✅ |
| A5 | 任意历史账期/配置版本可重放 100% | 用旧版本实测 | M3：30 天曲线+事件锚点重放走查；M5：红冲回滚走查——v2 内容原样保留（历史零修改）、生效值回到 v1（TC-M5-UI-05） | ✅ |
| A6 | 总览数据时效 ≤10s（可配置），超时项显式标注 | 现场实测 + UI 断言 | 同 A1：周期可配+三态标注已实现并走查验证；现场实测登记 U-API-01 | ⏳ |
| A7 | 权限隔离：用户仅见授权站点数据，100% 无越权 | 权限矩阵测试 TC-M1-* | Mock 适配器不做权限过滤（代码已注明）；权限矩阵已设计、ctx 参数留位，登记 U-UI-03 | ⏳ |
| A8 | 筛选/分页/返回上下文全组合可用，返回后状态保持 | UI 走查 | TC-M1-UI-03/06/11 + M2/M5/M6 回归走查均通过 | ✅ |
| A9 | 卡片趋势：断讯卡不渲染趋势；趋势随轮询滚动更新，100% 卡片一致 | UI 走查 + 逻辑断言 | TC-M1-UI-08（98 sparkline、0 空 polyline、24 点/线）/ UI-09（断讯等高占位不跳动）/ UI-10（隐藏页节流下滚动窗恒 24） | ✅ |
| A10 | 面板覆盖度：原型 8 面板全部有生产对应或显式裁剪留痕 | 面板核对表 + 走查 | TC-M2-UI-01：头部档案区 + 7 内容面板 + 参考值徽标 ×4；裁剪项留痕见《06-二期裁剪留痕》 | ✅ |
| A11 | 时效三态：每面板带采集时间与 ok/stale/gap 标注；断讯单元整页明示不造假 | UI 断言 + 逻辑测试 | TC-M2 逻辑 + 走查（M2-断讯页-UPS-A07.png） | ✅ |
| A12 | 告警区只读：M2 阶段零写入口，CI 静态检查可证 | scripts/check 扩展 | r2 unit 段禁 ack 处理器扫描；红线 5/5 通过 | ✅ |
| A13 | 返回上下文：详情→返回，总览筛选/页码保持（M1 回归不破） | 走查 | TC-M2 走查 + M1/M5/M6 回归走查 | ✅ |
| A14 | 历史跨度覆盖：三指标任一查 30 天 | UI 断言 + 接口断言 | TC-M3 走查（M3-历史页-30d缺口与事件.png） | ✅ |
| A15 | 历史缺口显式：断讯时段留白+图例标注，零插值连线（R3） | UI 走查 | M3 走查：缺口段留白+图例，无插值；REQ-M3 逻辑测试锚定 | ✅ |
| A16 | 事件锚点：市电中断/切换成功事件在曲线上可定位时刻 | UI 断言 | M3 走查：30 天视图事件标注与曲线同框 | ✅ |
| A17 | 深链/返回：#/ups/history/{id} 直达；返回 M2 上下文保留 | 走查 | M3 走查通过 | ✅ |
| A18 | 确认幂等：重复提交（双击/重试）数据层零副作用 | TC-M4-* 逻辑 | TC-M4 逻辑：已确认行重复提交被守门拒绝，数据层零副作用 | ✅ |
| A19 | 留痕完整率：100% 确认带操作者/时刻/备注，审计可抽查 | M6 审计流断言 | TC-M4 逻辑 + TC-M6-UI-02（审计行字段完整） | ✅ |
| A20 | 守门拦截：不存在/已确认/空备注/越权 100% 拦截且明示理由 | 逻辑测试 + 走查 | TC-M4 逻辑四路全覆盖 + TC-M4-UI 走查（理由逐条明示） | ✅ |
| A21 | 状态机单向：已确认不可再变更（UI 与数据层双不可写） | 逻辑测试 + CI | TC-M4 逻辑（applyAck 数据层拒绝）+ r2（已确认行无按钮 UI） | ✅ |
| A22 | 守门拦截率：无效配置 100% 不可发布，理由逐条明示 | TC-M5-* + 走查 | 同 A3 | ✅ |
| A23 | 版本不可变 + 回滚重放：历史版本零修改；回滚后旧版内容 100% 重放 | 逻辑测试 + 走查 | TC-M5-UI-05：回滚生成 v3 红冲版，v2 原样保留，生效值精确回放 | ✅ |
| A24 | 配置审计留痕：发布/回滚 100% 带操作者/时刻/摘要 | M6 审计流断言 | TC-M6-UI-02：真实点击发布后审计行完整（含操作者动态收集） | ✅ |
| A25 | 草稿/生效隔离：编辑与校验全程不影响生效值 | 逻辑测试 + 走查 | TC-M5-UI-02/03：守门失败生效值不动；发布前草稿区独立 | ✅ |
| A26 | 审计可查率：M4/M5 发射事件 100% 可查（流式回放断言） | TC-M6-* + 走查 | TC-M6 逻辑（四维筛选+seq 倒序）+ TC-M6-UI-02 端到端 | ✅ |
| A27 | 审计不可变：视图零修改/删除入口，CI 静态检查可证 | scripts/check r2 扩展 | r2 audit 段禁写 handler 扫描（7 词）；红线 5/5 通过 | ✅ |
| A28 | 筛选组合：类型/操作者/时间档/关键字全组合可用 | 走查 + 逻辑断言 | TC-M6-UI-03/04/05 走查 + 逻辑边界锚点注入全量覆盖 | ✅ |

## 对账汇总

- 已闭环：**25 / 28**（Mock 环境，证据可复跑）
- 待 FIN 实例闭环：**3 / 28**（A1/A6 → U-API-01；A7 → U-UI-03），明细见《03-配合事项与阻塞风险》
- 压测与容量（200 单元/首屏 ≤3s，U-INT-01）：需真实数据通道，列入实例就绪后补验
