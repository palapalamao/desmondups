# 需求到交付方法论 v1.0

| 项 | 内容 |
|---|---|
| 文档名称 | 需求到交付方法论（行业管控类平台软件） |
| 版本 | V1.0 |
| 日期 | 2026-09-18 |
| 溯源 | 提炼自 careBalance（院衡）项目全套文档：业务方案 AI4B-CB-BP-2026-001、详细设计 AI4B-CB-DD-2026-001 V1.1、careBalance-dev-docs（CLAUDE.md + 00~15）、cb 语义库与 UML 包 |
| 适用范围 | 有安全/合规硬约束、多外部系统对接、需组织多方会签、交付物需审计追溯的行业平台（能源、后勤、楼宇、工业管控等） |

---

## 总纲

> 先把领域的失败模式和否决者搞清楚（业务层）→ 把口径和语义固化（概念层）→ 把边界和守门机制立起来（逻辑层）→ 把一切写成无歧义契约（契约层）→ 按组织就绪度排程（计划层）→ 用机器守住红线写代码（实现层）→ 对照开头写下的标准交付（验收层）。

两条公理：

1. **可信度不来自永远正确，而来自永远诚实**——缺口不藏、不足标出、失败可见。
2. **承诺必须可被机器验证**——口头承诺不可靠，代码评审会疲劳，CI 不会。

---

## 第一部分 · 七阶段流程

每阶段按固定格式给出：做什么 / 产出 / 门禁（进入下一阶段的放行条件）。

### 阶段 0 · 底座盘点（开工前）

**做什么**

- 确认项目站在什么平台/内核之上，实地探测底座的真实能力（版本、类、API），不凭记忆写契约。
- 下"不重复造轮子"的纪律决定：哪些复用、哪些扩展、哪些全新。

**产出**：能力复用清单；底座探测记录（careBalance 实例：`_extracted/` 下的 listpods / listtypes / HxTypeList 探测脚本）。

**门禁**：底座关键能力逐项验证存在；对底座"不改写"的边界已书面确认。

### 阶段 1 · 业务建模（需求文档）

**做什么**——按固定顺序回答五个问题：

1. **这个领域和我熟悉的领域有什么结构性差异？** 产出差异对照表，每条差异必须推导出一个设计结论。（careBalance 实例：医院 vs 商业建筑七维差异表）
2. **这个领域最常见的死法是什么？** 产出失败模式清单，后续全部设计围绕规避它们。（实例：① 能耗分不到科室 → 财务不认账 → 平台沦为一块大屏；② 节能动作触碰感控红线 → 被一票否决 → 组织从此不敢用平台）
3. **不可妥协的原则是什么？** 产出设计铁律（实例：保障优先 / 成本可分摊 / 指标对齐考核）。
4. **谁在用、解决什么问题？** 产出角色地图 + 功能模块表，每个模块按「使用者 / 解决的问题 / 核心功能」三段式描述，模块编号（如 M1~M11）即需求条目主键。
5. **怎么算交付成功？** 量化验收标准**此时写死**（实例：数据完好率 ≥ 98%、缺口率 < 5%、财务书面确认分摊可用于成本核算、审计可追溯），不是最后补。

**产出**：《业务方案》（= PRD，业务语言，面向决策层签字；明确声明不覆盖技术详设）。

**门禁**：决策层签字；验收标准双方确认；每条失败模式都有对应规避设计。

### 阶段 2 · 概念与口径治理

**做什么**

- 把业务世界建成语义模型：对象、属性、枚举、关系，一域一文件，可直接落成代码。
- **把争议口径抢在写代码前固化**：枚举所有口径争议项（分母算不算 X、收入含不含 Y），以「口径确认单」形式多方会签落库；未会签的指标拒绝计算或标注参考值。
- 把治理要求编译进数据模型：约束必填依据文号、关键对象强制只读标记、外部编码必须经映射层。

**产出**：语义库（实例：`lib/cb/*.xeto`）+ 建模铁律 + 口径确认单模板。

**门禁**：语义库 lint 通过；关键口径全部有会签路径（人可未签，机制必须先建好）。

**要点**：指标算错通常不是算法错，是口径没签。口径治理属于需求阶段的尾巴，不属于开发阶段。

### 阶段 3 · 逻辑设计（详设）

**做什么**

1. **先划边界**：复用边界表逐项列出「直接复用 / 继承扩展 / 全新开发」，并写明负需求（不做什么）。
2. **再定分层**：表现层 → 服务接口层 → 应用层 → 内核 → 持久层 → 接入层，配硬约束（下层访问唯一出口化，如一律经 Repo）。
3. **识别系统的一票否决者**：把安全/合规否决逻辑独立成守门引擎——fail-closed（拿不到证据即拒绝）、否决是正常业务结果（非异常）、通过与否决全量留痕。
4. **定并发与事务模型**：调度串行化、幂等键、无跨记录事务时的补偿（红冲）策略、长任务进度回写。
5. 画关键流程时序图；定非功能指标（容量、性能、数据完好率）。

**产出**：《详细设计说明书》+ UML 图包（图源码与渲染图双格式）。

**门禁**：每个需求模块（M 编号）都能追溯到设计元素；每条铁律都有对应的架构机制而不只是口号。

### 阶段 4 · 契约化（开发文档）

**做什么**——把设计展开成无歧义契约，标志是"开发拿到任何一篇都能直接写代码，不需要回来问"：

- 入口一页纸：N 条绝对不能违反的红线 + 仓库结构 + 命名速查（实例：CLAUDE.md 七条红线）。
- 编码规范先行：语言规范、提交规范、scope 列表预设文档分工（00 篇）。
- 领域类：每个类写明构造期不变式（非法状态构造即抛错）。
- 持久层唯一出口，写入校验逐条列表（场景 / 校验 / 失败错误码）。
- 核心算法：伪代码 + 单元测试用例表（逐条写明期望结果）。
- 接口契约：分页、排序、幂等、错误响应格式全部定死；REST 与其他暴露面（如函数库）共享同一引擎实现，不存在两套逻辑。
- **错误码全表独立成篇**——异常路径是一等设计产物。
- 前端契约：路由、权限矩阵、强制渲染规则（未核证数据徽标、只读对象不渲染写入口——是"不渲染"而非"禁用"）。

**产出**：开发文档全套（实例：dev-docs 00~13 篇）。

**门禁**：任一契约无歧义；每条红线都标明了由哪层机制（断言/评审/CI）强制执行。

### 阶段 5 · 计划与排程

**做什么**

- 任务拆解到"可直接开 issue"的粒度，按里程碑组织。
- 排序依据是**技术依赖 × 组织就绪度**两个维度，不只是技术依赖。（实例：守门引擎技术上是核心，却排第四期——它依赖"关键区域清单三方会签"这件需要数月的组织工作；此前走人工流程兜底）
- 每个阻塞风险单独列表：风险 / 阻塞的里程碑 / 缓解方案，组织依赖要写明"谁去推动"。

**产出**：里程碑表 + 阻塞风险表（实例：dev-docs 15 篇，D1~D9）。

**门禁**：每个里程碑既标技术依赖也标组织依赖；每个风险有兜底路径。

### 阶段 6 · 实现（编码纪律）

1. **红线机器化**：对客户的关键承诺一律翻译成 CI 静态检查脚本（实例：`check-readonly-adapter.sh`、`check-no-point-write.sh`），构建失败比代码评审可靠。
2. **防御性默认（fail-closed）**：必填字段缺失即抛错不兜底；取不到实测值视为不满足；找不到归属对象按最高风险等级处理；不存在"先执行不冲突部分"的折中。
3. **数据诚实**：账目不平衡即阻断（实例：分摊三段之和与可分摊总量容差 0.01%，不过即阻断关账）；缺口永不摊入；未确认数据强制标注"参考值"；人工填报必须标来源等级。
4. **历史不可改**：映射/规则版本化（只新增带新生效期的版本）；修正走红冲不覆盖历史；保留原始外部编码（多存两列字段，换"不需重新向外部系统取数"的回溯能力）；一切写操作幂等——保证任意历史账期可无损重算。
5. **工程纪律**：引擎无状态（上下文显式传入）；持久层唯一出口；错误统一收敛为单一类型，对外不暴露堆栈；调度经统一 Actor 串行化，不自建并发池。
6. **降级路径先于主路径**：外部接口不可达时的人工兜底方案是设计的一部分（实例：HIS 不可达 → 人工填报 → 指标自动标注参考值，流程不中断）。

**门禁**：CI 含全部红线检查且为构建必经环节；核心算法测试用例全覆盖契约层用例表。

### 阶段 7 · 验证与交付

**分层验证**：单元（不变式与算法）→ 集成（关键业务全链路各走一遍，实例：造数 → 取数 → 映射 → 关账 → 分摊 → 出账单 → 算指标 → 异议 → 会签）→ 接口（契约 + 幂等 + 错误码）→ 现场验收。

**三个收官动作**：

1. **验收标准回收**：阶段 1 写下的每条标准逐条对账。
2. **权限与能力移交**：客户获得全部管理权限；强制审计操作现场演示可追溯。
3. **文档回路闭合**：开发中与上游文档的一切冲突回写上游，不留两套真相。

**门禁**：验收标准全部签字回收；压测与容量验证达标；文档链无未回写的漂移。

---

## 第二部分 · 三条贯穿机制（灵魂）

| 机制 | 含义 | careBalance 实例 |
|---|---|---|
| **约束硬化链** | 每条核心约束必须在四层各出现一次：宣言（铁律）→ 原则（详设）→ 契约（断言/红线）→ 机器（CI/测试）。人读前两层，评审看第三层，机器守第四层 | "平台永不写控制点"：三条设计铁律 → 详设"保障守门前置"原则 → 七条红线第 1 条 + GuardEngine 三条硬规则 → `check-no-point-write.sh` CI 扫描 + 反射测试 |
| **验收前置与回收** | 成功标准在需求阶段写死，中间所有设计决策用它裁决，交付阶段逐条回收 | 业务方案第 9 章 → test-plan 验收表，首尾呼应 |
| **真相单一、权威下沉、冲突回写** | 每层文档只说自己该说的并声明不覆盖什么；越靠近代码越权威；冲突必须回流修正上游，不允许漂移 | "冲突时以 dev-docs 为准，并回写上游文档" |

---

## 第三部分 · 五层文档漏斗（文档流水线）

```text
① 业务层   为什么做 / 为谁做 / 不做什么 / 怎么算成   → 《业务方案》（= PRD）
② 概念层   业务世界里有什么对象、什么口径           → 语义模型章 + 语义库源码 + 口径确认单
③ 逻辑层   系统怎么组织、职责怎么切分               → 《详细设计》：架构、边界表、时序、非功能
④ 契约层   精确到可编码的接口与规则                 → 开发文档：规范、领域、算法、接口、错误码、前端
⑤ 执行层   谁先谁后、怎么验证、何时算完             → 任务拆解 + 测试计划 + CI 脚本
```

配套规则：

- **单向承接**：每层只增加一类决策；每层文档开头声明"不覆盖什么"。
- **模块编号即需求主键**：M1~Mn（需求）→ 引擎/接口（设计）→ 里程碑（计划）→ 测试用例（验证），全链可追溯。
- **验收标准在开头写、在结尾收**。
- **派生物单向导出**：对客户/决策层的汇报材料（PPT 等）从业务方案单向导出，是叶子节点，不参与依赖链。
- **语义库与 UML 图源码入版本库**：图源码 + 渲染图双格式，可重建。

---

## 第四部分 · 注意事项速查清单

| # | 事项 | 做法 |
|---|---|---|
| 1 | 别抄方案 | 先做领域结构性差异分析 + 失败模式复盘，每条差异推出一个设计结论 |
| 2 | 硬约束引擎化 | 独立守门引擎，fail-closed，否决是正常业务结果且全量留痕 |
| 3 | 承诺可验证 | CI 静态检查 + 反射测试，不止代码评审 |
| 4 | 数据宁缺毋假 | 缺口/未映射/参考值全部显式标注；不平衡即阻断 |
| 5 | 口径先于算法 | 争议口径启动阶段多方会签固化，未会签拒算或标参考值 |
| 6 | 里程碑看组织就绪度 | 依赖会签/对照表的功能后置，先走人工流程兜底 |
| 7 | 历史不可改 | 版本化 + 红冲 + 幂等 + 保留原始编码，任意历史账期可无损重算 |
| 8 | 边界表格化 | 复用/扩展/全新逐项列清；写明负需求；不改写上游内核 |
| 9 | 集成最小化 | 只读、聚合、文件交付、网络分区、数据结构禁止敏感标识字段 |
| 10 | 防御性默认 | 无证据即拒绝；拿不到归属按最高风险等级处理 |
| 11 | 点表/数据留冗余 | 应急与监管关键点位设计期必留，事后补测代价极高 |
| 12 | 验收前置 | 量化验收标准需求阶段写死，反向定义"完成" |
| 13 | 异常是一等公民 | 错误码全表独立成篇；降级路径先于主路径设计 |
| 14 | 文档有门禁 | 文档 lint 配置、图源码入库、分发快照与工作副本分离 |
| 15 | 冗余即风险 | 同一内容多处副本时必须规定权威方并回写同步 |

---

## 第五部分 · 阶段检查清单（可直接勾选）

### 阶段 1 放行检查

- [ ] 领域结构性差异表完成，每条差异有设计结论
- [ ] 失败模式清单完成，每条有规避设计
- [ ] 设计铁律 ≤ 5 条，决策层认可
- [ ] 功能模块表（使用者/问题/功能三段式）完成并编号
- [ ] 量化验收标准双方书面确认

### 阶段 2 放行检查

- [ ] 语义模型 lint 通过
- [ ] 建模铁律成文
- [ ] 全部口径争议项有确认单与会签路径
- [ ] 治理要求（依据文号、只读标记、映射层）已编译进数据模型

### 阶段 3 放行检查

- [ ] 复用边界表完成（复用/扩展/全新 + 负需求）
- [ ] 分层图 + 跨层访问硬约束成文
- [ ] 守门引擎独立，fail-closed，留痕方案明确
- [ ] 并发/事务/幂等/补偿模型成文
- [ ] 每个需求模块可追溯到设计元素

### 阶段 4 放行检查

- [ ] 红线一页纸发布，全员已读
- [ ] 每个领域类有构造期不变式
- [ ] 持久层唯一出口 + 写入校验表
- [ ] 核心算法伪代码 + 测试用例表
- [ ] 接口契约无歧义（分页/幂等/错误格式）
- [ ] 错误码全表发布
- [ ] 前端权限矩阵 + 强制渲染规则

### 阶段 5 放行检查

- [ ] 任务可直接开 issue
- [ ] 每个里程碑标注技术依赖与组织依赖
- [ ] 阻塞风险表：风险/影响/缓解/责任人

### 阶段 6 放行检查

- [ ] 红线 CI 检查全部上线且为构建必经
- [ ] 核心算法测试用例全覆盖
- [ ] 降级路径已实现并测试
- [ ] 历史重算能力已验证（用旧账期实测）

### 阶段 7 放行检查

- [ ] 集成全链路测试通过
- [ ] 验收标准逐条回收签字
- [ ] 权限移交 + 审计可追溯现场演示
- [ ] 文档冲突全部回写上游，无漂移

---

## 附录 · 文档骨架模板（新项目套用）

```text
项目根/
  CLAUDE.md              # 入口：N 条红线 + 仓库结构 + 命名速查
  docs/
    01-业务方案.docx      # 阶段 1 产出（= PRD）
    02-详细设计.docx      # 阶段 3 产出
    dev/                 # 阶段 4 产出（开发文档）
      README.md          # 索引与阅读顺序
      00-conventions.md  # 编码与提交规范
      01-architecture.md # 分层 + 边界表 + 并发
      02-<domain>-lib.md # 语义库
      03-domain.md       # 领域类不变式 + Repo 契约
      04..07-*.md        # 引擎与核心算法（含测试用例表）
      08-api.md          # 接口契约
      11-errors.md       # 错误码全表
      12-scheduler.md    # 调度/事务/幂等
      13-frontend.md     # 路由/权限/强制渲染
      14-test-plan.md    # 测试 + CI + 验收回收
      15-tasks.md        # 里程碑 + 阻塞风险
    lib/<domain>/*.xeto  # 语义库源码（阶段 2 产出）
    fig/*.puml + *.png   # 图源码 + 渲染图
  scripts/check-*.sh     # 红线 CI 检查
```
