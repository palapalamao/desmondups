# CLAUDE.md — desmondUPS 项目入口

> 依据《需求到交付方法论 v1.0》。本文档是仓库权威入口；
> 冲突时以 dev/ 下契约层文档为准，并回写上游（真相单一、权威下沉、冲突回写）。

## 项目一句话

把 UPS 车队监控 UI 原型（Ractive 单页）交付为 FIN Framework 5.3.0 上的正式扩展模块（ext pod）。

## 红线（草案，v0.1，待阶段 4 契约层固化并配 CI 脚本）

1. **永不绕过 FIN 授权模型**：所有读写经 FIN 会话与权限服务，前端不自带认证。
2. **只读优先**：UPS 设备点位的默认集成方式是只读；任何写操作（配置下发、告警确认）必须走守门引擎，fail-closed，全量留痕。
3. **数据宁缺毋假**：无实测值显式标"缺口"，估算值显式标"参考值"，禁止静默兜底。
4. **不改写上游内核**：只扩展不补丁；FIN 内核、Haxall、Ractive 版本以 build.fan 锁定。
5. **历史不可改**：配置/映射版本化，只新增带生效期版本；修正走红冲；写操作幂等。

## 仓库结构

| 路径 | 阶段 | 内容 |
|---|---|---|
| `需求到交付方法论-v1.0.md` | — | 方法论总纲（外部输入，只读参考） |
| `docs/00-阶段0-底座探测记录.md` | 0 | 底座探测与能力复用清单 |
| `docs/01-业务方案/` | 1 | 业务方案（= PRD，待起草） |
| `docs/02-详细设计/` | 3 | 详细设计（待起草） |
| `docs/dev/` | 4 | 契约层开发文档（规范/领域/算法/接口/错误码/前端/测试） |
| `lib/ups/*.xeto` | 2 | 语义库源码（待建模） |
| `docs/fig/*.puml + *.png` | 3 | 图源码 + 渲染图，双格式入库 |
| `scripts/check-*.sh` | 6 | 红线 CI 检查脚本 |
| `UI prototype/AI-CES-IoT-UPS/` | — | 现有 Ractive 原型（视觉/交互基线，非生产代码） |
| `extensions/` | 6 | FIN ext pod 源码（待创建） |

## 命名速查

- 模块编号：M1~Mn（需求主键，全链追溯：需求→设计→里程碑→测试用例）
- 需求条目：`REQ-M<n>-<seq>`；测试用例：`TC-M<n>-<seq>`
- 里程碑：`D1~Dn`；错误码：`UPS-<4位数字>`
- 分支：`main` 受保护；功能分支 `feat/M<n>-<slug>`

## 文档纪律

- 每层文档开头声明"不覆盖什么"；每层只增加一类决策（五层漏斗）。
- 量化验收标准在业务方案阶段写死，交付阶段逐条回收。
- 图源码（puml）与渲染图（png）同目录入库，可重建。
